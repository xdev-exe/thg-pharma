PORT=8877
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=thg_pharma
DATABASE_URL="mysql://root:@localhost:3306/thg_pharma"
CORS_ORIGIN="http://localhost:3000"

# ── ERPNext ───────────────────────────────────────────────────────────────
# All three required for ERP sync. Generate in ERPNext:
#   Settings > Integrations > API Access > Generate Keys
# The user must have Sales Order (read+write) and Customer / Address (read+write) permissions.
ERP_URL=http://localhost:8000
ERP_KEY=
ERP_SECRET=
ERP_COMPANY=Zabbtnalk

# ── Order Notification Service (/order-notify) ─────────────────────────────
# Automatically notifies your team WhatsApp numbers when a new order is received
# or synced to ERPNext.
ORDER_NOTIFY_URL=http://localhost:3000/order-notify
ORDER_NOTIFY_SECRET=
